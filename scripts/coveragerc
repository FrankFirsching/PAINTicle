# .coveragerc to control coverage.py
[run]
omit =
    # omit anything in a .local directory anywhere
    */.local/*
    # and everything from the blender cache
     blender_cache/*

[report]
sort = Cover
